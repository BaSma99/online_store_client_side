class ProductCart{
  final productName;
  final productPicture;
  final size;
  final price;
  final ID;
  final color;
  final store;

  ProductCart(this.productName, this.productPicture, this.size, this.price,
      this.ID, this.color, this.store);
}